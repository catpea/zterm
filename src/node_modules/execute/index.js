const { ipcRenderer } = require('electron');

module.exports = function({line, term}){

  if(line == 'debug') return ipcRenderer.send('openDevTools');
  term.write(`src/node_modules/execute/index.js sucessfully pretended to execute: ${line}`);
  term.write(`\r\n`);
  console.log(line);

};
